# ============================================
# KYC Analyzer - Local Development Environment
# ============================================
# Copy this file to .env and fill in your values
# Never commit .env to version control!
#
# Usage: docker-compose up --build

# ============================================
# Environment Mode
# ============================================
# LOCAL = Read secrets from this file
# CLOUD = Fetch secrets from Azure Key Vault (production)
ENV=LOCAL

# ============================================
# LLM Provider Selection (choose one)
# ============================================
# Options: openai, azure, anthropic, ollama
LLM_PROVIDER=openai

# ============================================
# OpenAI Configuration
# ============================================
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1

# ============================================
# Azure OpenAI Configuration (if LLM_PROVIDER=azure)
# ============================================
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your-azure-openai-key
# AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# ============================================
# Anthropic Configuration (if LLM_PROVIDER=anthropic)
# ============================================
# ANTHROPIC_API_KEY=sk-ant-...

# ============================================
# Ollama Configuration (if LLM_PROVIDER=ollama)
# ============================================
# Free local models - no API key needed!
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.2

# ============================================
# RAG / Vector Search
# ============================================
RAG_ENABLED=true
EMBEDDING_MODEL=text-embedding-3-small

# ============================================
# Database (for local Docker Compose only)
# ============================================
# Note: These are used by docker-compose.yml
# In production (CLOUD mode), credentials come from Key Vault
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_NAME=app_db
DATABASE_USER=postgres
DATABASE_PASSWORD=localdevpassword123

# ============================================
# Azure Key Vault (CLOUD mode only)
# ============================================
# AZURE_KEYVAULT_URL=https://your-vault.vault.azure.net/
